// 1. 변수(값을 바꿔가면서 사용하는 저장소)

let age;
console.log(age);

age = 30;

console.log(age);

// 2. 상수(변하지 않는 값을 저장,초기화 반드시 필요, 초기화 = 초기값을 할당하는 것)

const birth = "1991.11.21";

// 3. 변수 명명 규칙
// $,_ 제외한 기호는 사용할 수 없다
// 변수의 이름은 숫자로 시작할 수 없다
// 예약어를 사용할 수 없다(ex)let let)
// 변수 명명 가이드
let a = 1;
let b = 1;
let c = a - b;
